# nwn2-utils

Utility library for Neverwinter Nights 2. Depends only on scripts included with the game.

## utils_flags.nss

To function this library requires that you create utils_flags.nss file that defines the following constants:

```nwscript
const int UTILS_LOG_WRITE_TO_DISK = TRUE;
```

